// ログインセッションを保持する期間(秒)。
// ログアウトするまで基本的にログイン状態を保ちたいため、ブラウザのCookieが
// 許容する上限(主要ブラウザで400日)に設定している。これより長い値を
// 指定してもブラウザ側で自動的に切り詰められるため、実質的な上限値。
export const SESSION_MAX_AGE = 60 * 60 * 24 * 400; // 約400日
